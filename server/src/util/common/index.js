const dotPathToObjectRef = require('./dot-path-to-object-ref');
const validateObject = require('./validate-object');

const common = {
    dotPathToObjectRef,
    validateObject
};

module.exports = common;
